# ============================================================
# AgentMape — Unified Requirements
# Install with: pip install -r requirements.txt
# ============================================================

# --- HTTP / Async ---
aiohttp>=3.8.0
aiohttp-cors>=0.7.0
requests>=2.28.0
websockets>=11.0

# --- Web Dashboard ---
Flask>=3.0.0
flask-cors>=4.0.0
flask-sock>=0.7.0

# --- Database ---
tinydb>=4.7.0

# --- YAML Handling ---
pyyaml>=6.0
ruamel.yaml>=0.18.0

# --- CLI / Terminal ---
click>=8.1.0
rich>=13.0.0

# --- Validation / Graph ---
jsonschema>=4.17.0
networkx>=3.0

# --- Data / Numeric (WorkflowValidator) ---
pandas>=2.0.0
numpy>=1.24.0

# --- LLM Providers ---
openai>=1.0.0

# --- Evaluator (council + aggregator) ---
# No extra dependencies — uses aiohttp + requests already listed above

# --- Image Validation (optional, WorkflowValidator) ---
Pillow>=10.0.0

# --- Testing ---
pytest>=7.4.0
pytest-cov>=4.1.0
